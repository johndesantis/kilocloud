# Client-side env vars are bundled into the app binary and are not secret.
#
# localhost works for local tooling and most simulator flows. When running on a
# physical phone, replace localhost with your development machine's LAN IP.
# On macOS, get the active LAN IP with:
# route -n get default | awk '/interface:/{print $2}' | xargs ipconfig getifaddr
API_BASE_URL=http://localhost:3000
WEB_BASE_URL=http://localhost:3000
CLOUD_AGENT_WS_URL=ws://localhost:8794
SESSION_INGEST_WS_URL=ws://localhost:8800
APPSFLYER_DEV_KEY=jnoVs6KzXanpbKrqXckPu9
APPSFLYER_APP_ID=6761193135
KILO_CHAT_URL=http://localhost:8808
EVENT_SERVICE_URL=ws://localhost:8809
NOTIFICATIONS_URL=http://localhost:8804
LATENCY_INGEST_URL=http://localhost:8816
POSTHOG_API_KEY=phc_GK2Pxl0HPj5ZPfwhLRjXrtdz8eD7e9MKnXiFrOqnB6z

# Native Google sign-in needs no local override: the OAuth client IDs live in the
# committed .env. They are public identifiers that already ship in the app binary
# and the iOS URL scheme. Do NOT add GOOGLE_WEB_CLIENT_ID / GOOGLE_IOS_CLIENT_ID
# lines here — @expo/env loads .env.local over .env, and a blank line shadows the
# real value, which drops the Google sign-in plugin from local prebuilds.

# Optional — override the Sentry environment tag for local runs (falls back to
# 'development' when unset on dev-client builds).
# EXPO_PUBLIC_SENTRY_ENVIRONMENT=development
